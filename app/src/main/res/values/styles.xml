<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="estiloBotones">
        <item name="android:layout_margin">@dimen/dimen10</item>
        <item name="android:background">@color/colorBoton</item>
        <item name="android:textColor">@color/colortextoBoton</item>
        <item name="android:textSize">@dimen/dimen20</item>
    </style>

</resources>
